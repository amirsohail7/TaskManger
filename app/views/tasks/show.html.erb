<div class="container mx-auto p-8">
  <h1 class="text-3xl font-semibold mb-8"><%= @task.name %></h1>

  <div class="bg-white p-8 rounded shadow mb-6">
    <strong class="text-gray-700 text-sm font-bold mb-2 block">Description:</strong>
    <p class="text-gray-700"><%= @task.description %></p>
  </div>

  <div class="flex items-center">
    <%= link_to 'Back', tasks_path, class: 'text-gray-500 hover:text-gray-700' %>
  </div>
</div>
